{
  "name": "@oflabs44/cockpit-plane",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "migrate": "wrangler d1 migrations apply cockpit-plane --local",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.12.33",
    "@hono/zod-openapi": "^1.5.1",
    "drizzle-orm": "1.0.0-rc.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.118.0"
  }
}
