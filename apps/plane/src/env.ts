// Bindings the plane Worker has access to. Empty for this slice — no D1, no Durable
// Objects yet (docs/architecture.md §2.1 lists them; they arrive with the enrolment slice).
export interface Env {}
